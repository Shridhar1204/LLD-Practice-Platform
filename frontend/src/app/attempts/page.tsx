'use client';import {useEffect,useState} from 'react';import Link from 'next/link';import {api,Attempt} from '@/lib/api';
export default function Attempts(){const [a,setA]=useState<Attempt[]>([]);useEffect(()=>{api<Attempt[]>('/attempts').then(setA)},[]);return <main className="container"><div className="hero"><div className="eyebrow">Review</div><h1>Attempt history</h1><p>See what you submitted and use feedback to shape the next iteration.</p></div>{a.length===0?<div className="empty">No attempts yet. Pick a problem and start practicing.</div>:<div className="grid">{a.map(x=><Link className="card" href={`/attempts/${x.id}`} key={x.id}><span className="status">{x.status}</span><h3>{x.problem_id}</h3><p className="muted">{new Date(x.created_at).toLocaleString()}</p>{x.evaluation&&<><div className="score">{x.evaluation.overall_score}/5</div><p className="muted">{x.evaluation.summary}</p></>}</Link>)}</div>}</main>}
